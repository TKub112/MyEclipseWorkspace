package zad1;

public class Decorator implements Publikacja{

	 private Publikacja publikacja;
     
		public Decorator(Publikacja publikacja2) {
		publikacja2 = publikacja;
	}

		// standard constructors
	 	@Override
	    public String opis() {
	        return publikacja.opis();
	    }

}
